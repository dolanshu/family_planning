# 项目名称：家庭 Todo 清单（family_planning）设计文档

## 1. 问题陈述

做一个**移动端优先**的网页版 Todo 清单：多账号各自持有私人清单，同时可通过「家庭」配置让成员间共享同一批任务。数据以单个 `todo.json` 文件持久化在服务端，通过 Docker 一键发布，最终部署到带域名的云服务器（HTTPS），开发期在 WSL 局域网以 HTTP 调试。

## 2. 目标用户

家庭成员（2~10 人）。手机浏览器访问，偶尔桌面访问。诉求是「谁的活谁看得到，家里公共的活大家都能勾」，而不是复杂的项目管理。

## 3. 设计方案对比

### 方案 A：纯静态前端 + localStorage
- 优点：零后端、部署最简单
- 缺点：**无法多账号、无法家庭共享、数据不落服务端** —— 直接违反需求 6/7
- 复杂度：低 / 结论：❌ 不满足核心需求

### 方案 B：Node + Express，内存状态 + 原子写 `todo.json`
- 优点：满足需求 5（数据就是本地文件）；单进程无并发写冲突；镜像小；前后端同语言
- 缺点：数据量增大后内存占用上升（家庭场景可忽略）
- 复杂度：中 / 结论：✅ **推荐**

### 方案 C：Node + Express + SQLite
- 优点：事务、索引、并发健壮
- 缺点：违反需求 5「后台数据存储在本地文件 todo.json」；引入额外依赖
- 复杂度：中高 / 结论：⚠️ 不满足需求，仅作未来演进方向

### 推荐方案：B
理由：严格贴合「JSON 文件存储 + AJAX + Docker」的需求组合；用「内存态 + 串行化原子落盘」即可消除 JSON 文件存储的并发写风险，无需引入数据库。

## 4. 技术架构

- **前端**：原生 HTML/CSS/JS（无构建步骤），移动端优先，AJAX（`fetch`）调用 `/api`。参考「Todo清单」：顶部日期标题栏 + 个人/家庭 作用域切换 + 任务列表 + 底部悬浮「+」新增 + 底部弹层表单。**完整界面与交互规格见 [4.5 前端详细功能规格](#45-前端详细功能规格)**。
- **后端**：Node 20 + Express。静态托管 `public/`，JSON API 挂载于 `/api`。
- **存储**：单个 `todo.json`（Docker volume 挂载到 `./data`）。启动时载入内存，每次变更**串行化**写入：写临时文件 → `fsync` → `rename` 原子替换。
- **鉴权**：`bcrypt` 存密码哈希；`cookie-session` 签名加密 Cookie 仅存 `{userId}`（无状态，重启不掉登录）。`httpOnly` + `sameSite=lax`，HTTPS 下自动 `secure`。

### 数据模型（`todo.json`）

```json
{
  "version": 1,
  "users":    [{ "id":"u_1", "username":"alice", "passwordHash":"$2b$..", "familyIds":["f_1"], "createdAt":"..." }],
  "families": [{ "id":"f_1", "name":"张家", "ownerId":"u_1", "memberIds":["u_1","u_2"], "inviteCode":"AB12CD", "createdAt":"..." }],
  "tasks":    [{ "id":"t_1", "title":"买牛奶", "notes":"", "status":"waiting",
                 "scope":"personal|family", "ownerId":"u_1", "assigneeId":"u_2|null",
                 "familyId":"f_1|null", "dueDate":"2026-09-10|null", "priority":0,
                 "createdAt":"...", "updatedAt":"...", "completedAt":"..." }]
}
```

**任务状态 `status`**：`waiting`（等待）/ `doing`（进行中）/ `done`（完成），缺省 `waiting`。
- 状态置为 `done` 时自动写入 `completedAt`；从 `done` 迁出时清空 `completedAt`。
- 「待办」（open）= `waiting` + `doing`。**已完成任务不出现在缺省列表**，仅按 `done` 显式筛选时返回。
- 状态迁移由服务端校验，仅允许上述三值。

**任务指派 `assigneeId`**：
- `null` = **公共任务**（家庭全员共同负责，缺省值）；非 null = 指派给该成员。
- 仅 `scope === "family"` 可指派；`scope === "personal"` 时强制为 `null`。
- `assigneeId` 必须属于该家庭的 `memberIds`，否则拒绝。
- 指派**不改变可见性**——家庭任务始终对全员可见、全员可编辑（含状态变更、改派与删除）。指派只标记「谁负责」。

### 可见性规则（核心授权逻辑）

用户可读写任务 `T` 当且仅当：
- `T.scope === "personal" && T.ownerId === 用户.id`，**或**
- `T.scope === "family" && 用户.familyIds 包含 T.familyId`

家庭任务**全员可见、全员可编辑**，`assigneeId` 仅作负责人标记，用于筛选与统计，不用于鉴权。

**成员变更时的指派清理**（数据一致性）：
- 成员退出家庭 / 被 owner 移除时，将该家庭中指派给该成员的任务 `assigneeId` 重置为 `null`（回到公共任务），避免出现悬空指派。

### API 设计

| 方法 | 路径 | 说明 |
|---|---|---|
| POST | `/api/auth/register` | 注册（用户名 3-20 位、密码 ≥6 位），成功后下发 Cookie |
| POST | `/api/auth/login` / `/api/auth/logout` | 登录 / 登出 |
| GET | `/api/auth/me` | 当前用户，未登录 401 |
| GET/POST | `/api/families` | 我加入的家庭列表 / 创建家庭（返回邀请码） |
| POST | `/api/families/join` `{inviteCode}` | 凭邀请码加入 |
| POST | `/api/families/:id/leave` | 退出家庭 |
| GET | `/api/families/:id/members` | 成员列表 |
| GET | `/api/tasks` | 我可见的任务，支持组合筛选（见下方「任务查询参数」） |
| POST | `/api/tasks` | 新建任务（`status` 缺省 `waiting`，`assigneeId` 缺省 `null`） |
| PATCH | `/api/tasks/:id` | 修改（标题/状态/到期/优先级/备注/作用域/**指派人**）；`assigneeId:null` 即改回公共任务 |
| DELETE | `/api/tasks/:id` | 删除 |
| GET | `/api/reports/completed` | 完成任务统计报告，支持导出（见下方「报告接口」） |

**任务查询参数**（`GET /api/tasks`，各维度 AND，同维度多值 OR）

| 参数 | 取值 | 说明 |
|---|---|---|
| `status` | `open`（**缺省**）/`waiting`/`doing`/`done`/`all` | 状态筛选 |
| `scope` | `personal`/`family`/`all`（缺省） | 归属筛选 |
| `familyId` | 家庭 id | 限定某个家庭 |
| `assignee` | `all`（缺省）/`public`/`me`/用户 id（逗号分隔多选） | **按指派人**筛选。`public` = `assigneeId` 为 `null` 的公共任务 |
| `creator` | 用户 id（逗号分隔多选） | 按**创建者**筛选（次要维度，缺省不限） |
| `priority` | `0,1,2,3`（逗号分隔多选） | 按优先级筛选 |
| `dueFrom` / `dueTo` | `YYYY-MM-DD` | 到期日区间 |
| `overdue` | `true` | 仅已逾期（到期日 < 今天且未完成） |

**报告接口**（`GET /api/reports/completed`）

| 参数 | 取值 | 说明 |
|---|---|---|
| `from` / `to` | `YYYY-MM-DD` | 统计区间，按 `completedAt` 过滤 |
| `groupBy` | `day`/`week`/`month`/`year` | 趋势图粒度 |
| `scope` / `familyId` / `assignee` | 同上 | 与任务查询一致的维度（`assignee=public` 只看公共任务） |
| `format` | `json`（缺省）/`csv` | `csv` 时直接返回带 BOM 的下载流 |

`format=json` 返回：`{ summary:{total,created,completionRate,avgHours}, trend:[{bucket,count}], byUser:[{userId|"public",username|"全体",count}], byPriority:{0:n,1:n,2:n,3:n}, items:[...] }`。

**统计口径**：人员维度一律按 **`assigneeId`** 归集——`null` 归入「全体」桶（`userId:"public"`），非 null 归入对应成员；不再使用创建者。

### Docker 与 HTTPS

- `Dockerfile`：多阶段 `node:20-alpine`，非 root 用户，暴露 `3000`。
- `docker-compose.yml`：**开发/局域网**用，映射 `3000`，数据卷 `./data:/app/data`。
- `docker-compose.https.yml` + `Caddyfile`：**生产**用（`--profile https`），Caddy 反代并自动申请 Let's Encrypt 证书；后端启用 `TRUST_PROXY=1` 以正确识别 HTTPS 并下发 `secure` Cookie。

## 4.5 前端详细功能规格

**总体**：单页应用（无构建步骤）。四个视图由 JS 切换 —— `Auth`（未登录）/ `Main`（清单 + 筛选）/ `Family`（家庭管理）/ `Report`（统计报告与导出）。所有数据经 `fetch('/api/...', { credentials:'same-origin' })` 获取。

### S1 登录 / 注册

- 顶部：App 名称「家庭清单」+ 副标题
- 卡片式表单：用户名、密码（字号 16px，防 iOS 自动放大）
- 主按钮「登录」；下方文字链「还没有账号？立即注册」
- 切到注册态：标题变「注册」，追加「确认密码」字段
- 前端校验：用户名 3–20 字符；密码 ≥6；两次输入一致
- 服务端错误：表单下方红色横幅（「用户名已存在」「用户名或密码错误」）
- 提交时按钮 loading + 禁用，防重复提交；成功后拉取任务进入 Main

### S2 主清单（核心页）

自上而下布局：

1. **顶栏**：左侧大标题显示今天「9月7日 星期一」，小字「你好，alice」；右侧 **「漏斗」筛选按钮** + 头像菜单（统计报告、家庭管理、退出登录）
2. **作用域 Tab**：横滑 chip ——「个人」「张家」（每个已加入家庭一个）。切换即过滤列表，选择记忆到 `localStorage`
3. **状态筛选 chip 行**：`待办`（**默认选中**，`waiting`+`doing`）/ `等待` / `进行中` / `已完成`（`done`）。**已完成任务只在切到「已完成」时出现，缺省页面不显示**
4. **已生效筛选摘要行**：人员/日期/优先级任一非默认时才出现，形如「张三 ✕」「本周 ✕」「高优先级 ✕」，点 ✕ 单独清除
5. **任务列表**：按「已逾期 → 今天 → 明天 → 未来 → 无日期」分组，每组灰色小标题；只渲染当前筛选命中的状态
6. **FAB**：右下角圆形「+」，避开 iPhone 安全区

#### 筛选面板（点顶栏「漏斗」打开的 Bottom Sheet）

| 维度 | 选项 | 说明 |
|---|---|---|
| **指派人** | 全部 / **全体（公共）** / 我 / 各成员用户名 | 家庭作用域下可用；个人作用域隐藏该维度。**按 `assigneeId` 筛选；「全体（公共）」= `assigneeId` 为 `null`** |
| **到期日** | 全部 / 已逾期 / 今天 / 本周 / 本月 / 自定义起止 | 自定义时展开两个 `date` 输入 |
| **优先级** | 全部 / 高 / 中 / 低 / 无 | 支持多选（可同时勾「高」+「中」） |

- 面板底部：「重置」「查看结果」（主按钮）
- 维度之间为 **AND** 关系，同一维度多选为 OR
- 与顶部「状态 chip」叠加生效；筛选条件同步到 URL query，便于刷新/分享

任务条目（行高 ≥56px）：

- 左：**圆形状态圈**（44×44 触摸区）——未完成时空心圆，完成时实心圆 + 对勾（带勾选动画）。点击 = 标记完成 / 取消完成（取消后回到「等待」）
- 中：标题（完成态加删除线并置灰）；下方 meta 行依次为**状态徽章** + **指派人标记** + 到期日（逾期红色 / 今天橙色）。指派给某人时显示头像 + 姓名，公共任务显示灰色「全体」标
- **状态徽章**：`等待`(灰) / `进行中`(蓝) / `已完成`(绿灰)。未完成任务**点击徽章直接在 等待 ↔ 进行中 间切换**，无需打开弹层
- 左缘 4px 色条表示优先级：无=透明、低=灰、中=橙、高=红
- 右：有备注时显示备注图标
- 交互：点行主体 → 打开编辑弹层；左滑 → 露出红色「删除」；长按 → 同左滑
- 标记完成后，条目以 200ms 高度塌陷动画从「待办」列表移除

### S3 新增 / 编辑任务（底部弹层 Bottom Sheet）

- 底部滑出，圆角 16px，遮罩背景；点遮罩或下滑关闭
- 字段：
  - 标题（单行，自动聚焦）
  - 备注（多行，可选）
  - **状态：segmented「等待 / 进行中 / 完成」三选一**，新建默认「等待」，可直接设为任一状态
  - 归属：segmented「个人 / 家庭」，未加入家庭时家庭项禁用
  - **指派给**：chip 组「**全体（公共）**」+ 各家庭成员；**仅归属 = 家庭时显示**，默认「全体（公共）」。选成员即指派，选回「全体」即取消指派
  - 到期日：快捷 chip「今天」「明天」+ 日期选择器
  - 优先级：chip「无 / 低 / 中 / 高」
- 底部按钮：「保存」「取消」；编辑态额外提供红色「删除任务」
- 保存时按钮 loading，接口返回后刷新列表

### S4 家庭管理

- 我加入的家庭卡片列表：家庭名、成员数、成员用户名列表
- 「创建家庭」：输入名称 → 成功后大字等宽展示**邀请码** + 「复制」按钮
- 「加入家庭」：输入 6 位邀请码
- 每张卡片：「退出家庭」（二次确认）；owner 额外可见「重命名」「移除成员」

### S5 统计报告与导出

入口：顶栏头像菜单 → 「统计报告」。

**控制区**

- 作用域：沿用当前「个人 / 家庭」选择；家庭作用域下可再按**人员**筛选
- **时间粒度**：segmented「日 / 周 / 月 / 年」
- **时间区间**：左右箭头切换上一段 / 下一段，中间显示区间标题（如 `2026-09-07`、`2026 第 37 周`、`2026-09`、`2026`）；另提供「自定义范围」

**统计内容**（口径：`completedAt` 落在区间内的已完成任务）

1. **概览卡片**：完成任务数、区间内新增任务数、完成率、平均完成耗时（`completedAt − createdAt`）
2. **趋势图**：按所选粒度的完成数柱状图（纯 SVG/CSS 绘制，不引第三方图表库）
3. **按指派人分组**：成员 → 完成数，**并含「全体（公共）」一栏**，横向条形图 + 数值（个人作用域隐藏）
4. **按优先级分组**：高 / 中 / 低 / 无 各自完成数
5. **明细表**：区间内完成任务列表（标题、完成人、完成时间、优先级、耗时），点击可跳转编辑

**导出**

- 「导出 CSV」「导出 JSON」两个按钮
- 由**服务端**生成，保证与页面统计口径一致：`GET /api/reports/completed?...&format=csv`
- CSV 带 **UTF-8 BOM**，避免 Excel 打开中文乱码；列：`标题,状态,优先级,创建人,归属,创建时间,完成时间,耗时(小时),到期日`
- 文件名形如 `todo-report-2026-09-07_2026-09-13.csv`

**空态**：区间无完成任务时显示「该时间段没有已完成的任务」+ 切换区间提示。

### 移动端适配

- `viewport-fit=cover` + `env(safe-area-inset-*)` 适配刘海与 home 条
- 触摸目标 ≥44×44；`-webkit-tap-highlight-color: transparent`
- 输入框 `font-size ≥16px`；禁用双击缩放
- 支持深色模式 `prefers-color-scheme`
- 提供 `manifest.webmanifest` + 主题色，支持「添加到主屏幕」

### 视觉规范

- 主色 `#4F7CFF`，背景 `#F5F6F8`，卡片 `#FFFFFF`，圆角 12–16px
- 字体：`-apple-system / PingFang SC / Microsoft YaHei`；标题 20–24px、正文 16px、meta 13px
- 动效：弹层 200ms ease-out 上滑；勾选 150ms；删除 200ms 高度塌陷

### 状态与异常处理

- **空态**：插画 + 「还没有任务，点右下角 + 添加」；筛选无结果时「没有符合条件的任务」
- **加载**：首次进入骨架屏
- **401**：任意接口返回 401 → 清本地态、跳回登录页并提示「登录已过期」
- **网络失败**：顶部 toast「网络异常，请重试」+ 重试按钮
- **离线**：监听 `navigator.onLine`，顶部黄条提示

### 前端文件结构

```
public/
  index.html
  manifest.webmanifest
  css/style.css
  js/api.js      # fetch 封装，统一处理 401 与错误
  js/auth.js     # 登录/注册视图
  js/tasks.js    # 清单渲染与交互
  js/filter.js   # 筛选面板（指派人/日期/优先级）
  js/report.js   # 统计报告页与导出
  js/chart.js    # 轻量 SVG 柱状图/条形图
  js/family.js   # 家庭管理
  js/ui.js       # toast / sheet / confirm 通用组件
  js/app.js      # 启动与视图路由
```

## 5. 关键风险

| 风险 | 影响 | 缓解措施 |
|---|---|---|
| JSON 并发写导致文件损坏 | 数据丢失 | 内存态 + 串行写队列 + 临时文件 `rename` 原子替换；启动时若解析失败回退备份 |
| 生产环境 Cookie 未下发（反代未透传协议） | 登录后仍 401 | `TRUST_PROXY` 环境变量 + Caddy 标准头 |
| 公网开放注册被滥用 | 垃圾账号 | `ALLOW_REGISTRATION` 环境变量，可一键关闭 |
| 容器数据卷未备份 | 数据丢失 | 文档中明确 `./data/todo.json` 需定期备份 |
| 局域网 HTTP 下部分浏览器特性受限 | 剪贴板/PWA 不可用 | 开发期可接受；需要时加 Caddy 本地证书 |
| CSV 导出中文乱码 | 报表不可用 | 输出 UTF-8 BOM；导出后自检用 Excel 打开 |
| 报告时区不一致（服务器 UTC vs 用户本地） | 统计数字错位 | 全链路统一按服务器时区 `TZ`（默认 `Asia/Shanghai`），日期以 `YYYY-MM-DD` 字符串比较 |
| 悬空指派（成员退出后 `assigneeId` 指向非成员） | 统计出现幽灵人员 | 退出/移除成员时把该家庭相关任务 `assigneeId` 重置为 `null`；读取时二次校验 `assigneeId ∈ memberIds`，否则按公共任务处理 |
| 指派人与创建者口径混淆 | 统计数字对不上 | 统一以 `assigneeId` 为人员维度，`null` 归「全体」桶；创建者仅作次要筛选 `creator` |
| 区间过大导致导出慢 | 请求超时 | 报告接口限制最大区间（如 2 年）并返回 400；前端提示缩小范围 |

## 6. 成功标准

1. `docker compose up` 后，局域网手机浏览器可打开、注册、登录。
2. 两个账号各自看到**不同的**私人清单。
3. 两账号加入同一家庭后，能看到并**共同编辑**同一批家庭任务。
4. 任务可在「等待 / 进行中 / 完成」之间切换（条目圆点、状态徽章、编辑弹层三种入口）。
5. 标记「完成」的任务**立即从缺省列表消失**，仅在「已完成」筛选下可见；取消完成后回到「等待」。
6. 容器重启后数据完整保留（`todo.json` 落卷）。
7. 家庭任务可**指派给指定成员**，缺省为「全体（公共）」；可改派与取消指派；**指派不改变全员可见、全员可编辑**。
8. 成员退出家庭 / 被移除后，其名下指派任务自动回到「全体（公共）」，统计中不再出现该成员。
9. 可按**指派人（含「全体」）/ 到期日 / 优先级**组合筛选任务，多维度叠加结果正确（AND 跨维度、OR 同维度）。
10. 统计报告可按**日 / 周 / 月 / 年**切换粒度，数字与明细列表一致；区间无数据时显示空态。
11. 报告「按指派人分组」含「全体（公共）」一栏，与各成员完成数之和等于总完成数。
12. 导出的 CSV / JSON 内容与页面统计口径一致，CSV 用 Excel 打开中文不乱码。
13. 后端单元测试 + API 集成测试全部通过（覆盖可见性、鉴权、状态迁移、指派校验、筛选与报告聚合）。
14. `docker compose --profile https up` 在带域名环境下可取得有效 HTTPS 证书。
