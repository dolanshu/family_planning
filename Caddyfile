# Caddy 反向代理：自动为 {$DOMAIN} 申请并续期 Let's Encrypt 证书
{$DOMAIN} {
	encode zstd gzip

	reverse_proxy app:3000 {
		header_up X-Real-IP {remote_host}
	}

	header {
		Strict-Transport-Security "max-age=31536000"
		X-Content-Type-Options nosniff
		Referrer-Policy no-referrer
	}
}
